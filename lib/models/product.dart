class ProductItem {
  final String image, title, quantity, price, rating, sale;

  ProductItem({
    this.image,
    this.title,
    this.price,
    this.rating,
    this.sale,
    this.quantity,
  });
}
